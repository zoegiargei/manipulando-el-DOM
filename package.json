{
  "name": "proyecto-7-pruebas",
  "version": "1.0.0",
  "description": "gestor de proyectos",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "sass estilos.scss ./css/estilos.css -w --no-source-map -s compressed"
  },
  "author": "GIARGEI",
  "license": "ISC",
  "dependencies": {
    "bootstrap": "^5.2.0-beta1"
  }
}
